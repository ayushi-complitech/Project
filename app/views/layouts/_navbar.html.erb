 <nav>
    <div class="navbar navbar-dark navbar-static py-2">
    <div class="container-fluid">
      <div class="navbar-brand">
        <a href="index.html" class="d-inline-flex align-items-center">
           <%= image_tag src="logo_icon.svg" %>
          <%= link_to "Blogs", root_path, class:"fs-5 fw-bold ps-3 text-white" %>
        </a>
      </div>

      <div class="d-flex justify-content-end align-items-center ms-auto">
        <ul class="navbar-nav flex-row">
          <% if user_signed_in? %>
            <%= link_to current_user.email, edit_user_registration_path, class:"d-none d-md-inline-block ms-2 text-white pe-3" %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"d-none d-md-inline-block ms-2 text-white pe-3" %>
          <% else %>
           <div class="d-flex align-items-center mx-md-1">
            <%= link_to "Log in", new_user_session_path, class:"d-none d-md-inline-block ms-2 text-white" %>
           </div>
           <div class="d-flex align-items-center mx-md-1">
            <%= link_to "Register", new_user_registration_path, class:"d-none d-md-inline-block ms-2 pe-3 text-white" %>
           </div>
          <% end %>
        
        </ul>
      </div>
    </div>
  </div>
  </nav>
 